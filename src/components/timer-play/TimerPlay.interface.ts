export interface TimerPlayProps {
  // Estilos custom.
  customStyle: boolean
  // ID de la tarea con el Play.
  taskID?: number
  // Wording a mostrar en el tooltip.
  wordingTip?: string
}

// Style Component Props
export interface SCTimerPlayProps {
  customStyle?: boolean
}
