import styled from 'styled-components'
import { animationMixin } from '../../style'

export const SCL2L2PositionsSkeleton = styled.div`
  width: 180px;
  height: 30px;
  border-radius: 5px;
  ${animationMixin}
`
