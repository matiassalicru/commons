import styled from 'styled-components'
import { animationMixin } from '../../style'

export const SCAddButtonSkeleton = styled.div`
  width: 44px;
  height: 44px;
  border-radius: 22px;
  ${animationMixin}
`
